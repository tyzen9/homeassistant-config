# Monitor docker is configured to connect to the Docker API via socket-proxy
- name: !secret host_1_hostname
  containers_exclude:
    - "volume-backup"    
  url:  !secret host_1_socketproxy_tcp_url
  scan_interval: 600
  monitored_conditions:
    - allinone

- name: !secret host_2_hostname
  containers_exclude:
    - "volume-backup"    
  url:  !secret host_2_socketproxy_tcp_url
  scan_interval: 600
  monitored_conditions:
    - allinone

- name: !secret host_3_hostname
  containers_exclude:
    - "volume-backup"    
  url:  !secret host_3_socketproxy_tcp_url
  scan_interval: 600
  monitored_conditions:
    - allinone
